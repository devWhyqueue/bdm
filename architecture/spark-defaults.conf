# Spark Iceberg and S3A (MinIO) configurations
spark.sql.extensions                               org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions
spark.sql.catalog.spark_catalog                    org.apache.iceberg.spark.SparkSessionCatalog
spark.sql.catalog.spark_catalog.type               hadoop
# spark.sql.catalog.spark_catalog.warehouse explicitly set in SQL or spark-sql command

# Hadoop S3A settings for MinIO
spark.hadoop.fs.s3a.endpoint                       http://minio:9000
spark.hadoop.fs.s3a.access.key                     minioadmin
spark.hadoop.fs.s3a.secret.key                     minioadmin
spark.hadoop.fs.s3a.path.style.access              true
spark.hadoop.fs.s3a.impl                           org.apache.hadoop.fs.s3a.S3AFileSystem
spark.hadoop.fs.s3a.connection.ssl.enabled         false
# For Iceberg REST catalog if used (not in this plan, but good to be aware)
# spark.sql.catalog.rest_catalog.uri               http://rest-catalog-uri:8181
# spark.sql.catalog.rest_catalog.credential        user:password
# spark.sql.catalog.rest_catalog.warehouse         s3a://minio-bucket/trusted-zone/iceberg_catalog/
